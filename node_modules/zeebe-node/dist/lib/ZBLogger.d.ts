import { Loglevel, ZBLoggerOptions } from './interfaces';
export declare class ZBLogger {
    loglevel: Loglevel;
    private colorFn;
    private taskType?;
    private id?;
    private stdout;
    private colorise;
    private pollInterval?;
    private namespace;
    constructor({ loglevel, color, id, namespace, stdout, taskType, colorise, pollInterval, }: ZBLoggerOptions & {
        id?: string;
        colorise?: boolean;
    });
    info(message: any, ...optionalParameters: any[]): void;
    error(message: any, ...optionalParameters: any[]): void;
    debug(message: any, ...optionalParameters: any[]): void;
    log(message: any, ...optionalParameters: any[]): void;
    private makeMessage;
    private _colorise;
}
